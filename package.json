{
  "name": "futurus",
  "version": "0.0.2",
  "description": "A queue that lets you see into the future",
  "main": "index.js",
  "scripts": {
    "build": "tsc src/index.ts --outDir . --module commonjs --declaration --noImplicitAny",
    "prepublish": "npm run build",
    "test": "npm run build && nodeunit"
  },
  "author": "Evan Solomon",
  "license": "MIT",
  "dependencies": {
    "typescript": "^1.0.1"
  },
  "devDependencies": {
    "nodeunit": "^0.9.0"
  }
}
